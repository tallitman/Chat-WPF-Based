using System;

namespace DataAccessLayer
{
    class Program
    {
        static void Main(string[] args)
        {
/*
            string nickname = "gan";
            string group ="6";
            string password = "israel";
            UserHandler user = new UserHandler();
           //user.login(nickname, group, password);
       Console.WriteLine(user.register("gan", "6", "israel"));
        // Console.WriteLine(user.register("gan3", "7", "israel"));
        */
        }
    }
}
